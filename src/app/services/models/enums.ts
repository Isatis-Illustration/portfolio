export enum StorageKey{
    FILTER = 'filter',
    LANGUAGE = 'lang',
    DETAIL_ID = 'detail-id'
}

export enum Type{
    ILLUSTRATION = 'i',
    CHARACTER = 'c',
}

export enum Language{
    IT = 'it',
    EN = 'en',
}